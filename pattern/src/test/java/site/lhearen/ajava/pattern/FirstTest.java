package site.lhearen.ajava.pattern;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class FirstTest {
    @Test
    public void testSayHi() {
        String sayHi = "sayHi";
        assertEquals("sayHi", sayHi);
    }
}
